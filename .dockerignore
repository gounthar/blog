# The hugo.Dockerfile is self-contained (no COPY); the source tree is
# bind-mounted at runtime via docker-compose volumes. Keep the build context
# small by excluding generated output, VCS data, and dependencies.
.git
public
resources
node_modules

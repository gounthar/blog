---
date: 2020-08-13 00:00:00 +0000
author: Poddingue
categories:
- Linux
- Multimedia
description: Install an RTMP proxy to restream on Centos 7
draft: true
featured: true
hidden: true
image: assets/images/closed-coming-soon-door-4291-1210x423_1_.jpg
tags:
- centos
- ffmpeg
- nginx
- RTMP
title: Install an RTMP proxy to restream on Centos 7
---

== compile

taken from https://www.hostwinds.com/guide/live-streaming-from-a-vps-with-nginx-rtmp/

[source,bash]
----
cd /usr/src/
wget https://nginx.org/download/nginx-1.19.2.tar.gz && tar -xvzf nginx-1.19.2.tar.gz && rm nginx-1.19.2.tar.gz
git clone https://github.com/sergey-dryabzhinsky/nginx-rtmp-module.git
wget https://www.openssl.org/source/openssl-1.1.1g.tar.gz && tar -xvzf openssl-1.1.1g.tar.gz && rm openssl-1.1.1g.tar.gz
cd nginx-1.19.2/
./configure --with-http_ssl_module --add-module=../nginx-rtmp-module --with-openssl=../openssl-1.1.1g
make && make install
cat /usr/lib/systemd/system/nginx.service
[Unit]
Description=The nginx HTTP and reverse proxy server
After=network.target remote-fs.target nss-lookup.target

[Service]
Type=forking
PIDFile=/run/nginx.pid
== Nginx will fail to start if /run/nginx.pid already exists but has the wrong
== SELinux context. This might happen when running `nginx -t` from the cmdline.
== https://bugzilla.redhat.com/show_bug.cgi?id=1268621
ExecStartPre=/usr/bin/rm -f /run/nginx.pid
ExecStartPre=/usr/local/nginx/sbin/nginx -t
ExecStart=/usr/local/nginx/sbin/nginx
ExecReload=/bin/kill -s HUP $MAINPID
KillSignal=SIGQUIT
TimeoutStopSec=5
KillMode=process
PrivateTmp=true

[Install]
WantedBy=multi-user.target

systemctl daemon-reload
systemctl start nginx


----

== Systemd

Based on https://tylersguides.com/guides/installing-nginx-from-source-on-centos-7/

[source,bash]
----
groupadd -g 994 nginx

----
